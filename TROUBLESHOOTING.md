# دليل حل المشاكل

## ❌ خطأ: "حدث خطأ أثناء تحميل الصفحة"

### السبب المحتمل:
التطبيق يستخدم `fetch API` لتحميل الصفحات، وهذا لا يعمل مع بروتوكول `file://` في المتصفح.

### الحلول:

#### الحل 1: استخدام خادم ويب محلي (مُوصى به)

**خيار A: VS Code Live Server**
1. افتح VS Code
2. ثبت extension "Live Server"
3. انقر بزر الماوس الأيمن على `index.html`
4. اختر "Open with Live Server"

**خيار B: Python**
```bash
# في مجلد المشروع
python -m http.server 8000
# ثم افتح: http://localhost:8000
```

**خيار C: Node.js**
```bash
# في مجلد المشروع
npx http-server
# أو
npx serve
```

**خيار D: PHP**
```bash
# في مجلد المشروع
php -S localhost:8000
```

#### الحل 2: فتح الصفحات مباشرة (بدون Router)

إذا لم تستطع استخدام خادم ويب، يمكنك فتح الصفحات مباشرة:
- `pages/home.html`
- `pages/branches.html`
- `pages/sales.html`
- `pages/reports.html`

**ملاحظة**: في هذه الحالة، لن يعمل Router والتنقل بين الصفحات.

#### الحل 3: استخدام Chrome مع --allow-file-access-from-files

```bash
chrome.exe --allow-file-access-from-files
```

**تحذير**: هذا غير آمن ولا يُنصح به.

---

## 🔍 التحقق من المشكلة

افتح Console في المتصفح (F12) وتحقق من:

1. **خطأ Fetch**: 
   ```
   Failed to load page: pages/home.html
   ```
   → الحل: استخدم خادم ويب محلي

2. **خطأ في المسار**:
   ```
   Page content (.main-content) not found
   ```
   → تحقق من أن الملفات موجودة في المسار الصحيح

3. **خطأ في تحميل JavaScript**:
   ```
   Failed to load page script: home
   ```
   → تحقق من أن ملفات JS موجودة في `js/pages/`

---

## ✅ التحقق من البنية

تأكد من وجود الملفات التالية:

```
📁 المشروع/
├── index.html
├── pages/
│   ├── home.html
│   ├── branches.html
│   ├── sales.html
│   └── reports.html
├── js/
│   ├── core/
│   │   ├── config.js
│   │   ├── utils.js
│   │   ├── data.js
│   │   └── router.js
│   └── pages/
│       ├── home.js
│       ├── branches.js
│       ├── sales.js
│       └── reports.js
└── css/
    └── styles.css
```

---

## 🚀 الحل السريع

**الأسهل**: استخدم VS Code Live Server
1. افتح المشروع في VS Code
2. انقر بزر الماوس الأيمن على `index.html`
3. اختر "Open with Live Server"

سيتم فتح التطبيق على `http://localhost:5500` وسيعمل بشكل صحيح!









# 🚀 دليل التشغيل السريع

## الطريقة 1: استخدام ملف التشغيل (الأسهل) ⭐

### Windows:
- انقر نقراً مزدوجاً على `START_SERVER.bat`
- أو انقر بزر الماوس الأيمن → "Run with PowerShell" على `START_SERVER.ps1`

### ثم افتح المتصفح على:
```
http://localhost:8000
```

---

## الطريقة 2: من Terminal/Command Prompt

### باستخدام Python:
```bash
python -m http.server 8000
```

### باستخدام Node.js:
```bash
npx http-server
```

### ثم افتح:
```
http://localhost:8000
```

---

## الطريقة 3: VS Code Live Server (الأفضل للتطوير)

1. افتح VS Code
2. ثبت extension "Live Server"
3. انقر بزر الماوس الأيمن على `index.html`
4. اختر "Open with Live Server"

---

## ✅ التحقق من التشغيل

بعد تشغيل الخادم، يجب أن ترى:
- ✅ صفحة الرئيسية تظهر
- ✅ القائمة العلوية تعمل
- ✅ يمكنك التنقل بين الصفحات
- ✅ لا توجد أخطاء في Console

---

## 🛑 إيقاف الخادم

اضغط `Ctrl + C` في Terminal

---

## ❌ إذا لم يعمل

1. تأكد من أن Python أو Node.js مثبت
2. تحقق من أن المنفذ 8000 غير مستخدم
3. جرب منفذ آخر: `python -m http.server 8080`
4. راجع `TROUBLESHOOTING.md` للمزيد من المساعدة








